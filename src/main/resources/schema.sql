create schema if not exists bash;

create table Employee (
	id INT,
	first_name VARCHAR(50),
	last_name VARCHAR(50),
	email VARCHAR(50),
	gender VARCHAR(50),
	office_location VARCHAR(50),
	profile_pic VARCHAR(50),
	title VARCHAR(50)
);
